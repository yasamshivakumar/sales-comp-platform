# Netlify — React frontend only (API stays on Render)
# Site settings: https://app.netlify.com → your site → Build & deploy

[build]
  base = "frontend"
  # CI=false — CRA treats ESLint warnings as errors when CI=true (Netlify default)
  command = "npm install && CI=false npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"

# Create React App — client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
