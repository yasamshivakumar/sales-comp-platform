name: Backend CI (legacy redirect)

# Kept so older status-check names / bookmarks still resolve.
# The full pipeline lives in ci-cd.yml (build, test, package, deploy, monitor).

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use CI/CD workflow instead
        run: |
          echo "Backend tests now run in .github/workflows/ci-cd.yml"
          echo "Trigger the CI/CD workflow for build → test → package → deploy → monitor."
